#include "holberton.h"
#include <stdio.h>
/**
 * _supper - checks for uppercase character
 * Return: Always 0. 
 */
int _isupper(void)
{
  char c;
  c = 'A';
  printf("%c: %d\n", c, _isupper(c));
  c = 'a';
  printf("%c: %d\n", c, _isupper(c));
  return (0);
    }
}
